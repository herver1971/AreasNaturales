/*
This has been generated by the overpass-turbo wizard.
The original search was:
“boundary=protected_area and leisure=nature_reserve and relate_law=* and protect_class!=* in Argentina”
*/
[out:json][timeout:125];
// fetch area “Argentina” to search in
area(3600286393)->.searchArea;
// gather results
(
  // query part for: “boundary=protected_area and leisure=nature_reserve and relate_law=* and protect_class!=*”
  node["boundary"="protected_area"]["leisure"="nature_reserve"]["related_law"]["protect_class"!~".*"](area.searchArea);
  way["boundary"="protected_area"]["leisure"="nature_reserve"]["related_law"]["protect_class"!~".*"](area.searchArea);
  relation["boundary"="protected_area"]["leisure"="nature_reserve"]["related_law"]["protect_class"!~".*"](area.searchArea);
);
// print results
out body;
>;
out skel qt;
