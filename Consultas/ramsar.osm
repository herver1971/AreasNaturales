/*
This has been generated by the overpass-turbo wizard.
The original search was:
“ramsar=yes in Argentina”
*/
[out:xml]/*fixed by auto repair*/[timeout:125];
// fetch area “Argentina” to search in
area(3600286393)->.searchArea;
// gather results
(
  // query part for: “ramsar=yes”
  way["ramsar"="yes"](area.searchArea);
  relation["ramsar"="yes"](area.searchArea);
);
// print results
out meta;/*fixed by auto repair*/
>;
out meta qt;/*fixed by auto repair*/
